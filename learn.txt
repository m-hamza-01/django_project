-- make a django application 
python -m venv venv
venv/Scripts/activate
pip install django djangorestframework
django-admin startproject django_test . (start project)
python manage.py migrate     
python manage.py startapp api   -- ( making new apps)

venv/Scripts/activate  -- activate my virtual env

python manage.py ( leads to subcommands)

python manage.py runserver

-- making new apps 
everytime this happens it has to be added inside 
installed_apps inside settings.py

-- views is used for request and response 

-- the following is the debug toolbar 
https://django-debug-toolbar.readthedocs.io/en/latest/

-- django field types:
https://docs.djangoproject.com/en/5.2/ref/models/fields/

-- after the set up of mysql install the following:
pipenv install mysqlclient 


--go to settings.py inside your main module and then change the database, and set your password
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'django_test',
        'HOST':'localhost',
        'USER':'root',
        'PASSWORD': '222378'

    }
}

-- mockaroo.com ( is used for making dummy data)

django official documentation to check: 
-- https://docs.djangoproject.com/en/5.2/ref/contrib/admin/ 